<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 基础主题 -->
    <style name="Base.Theme.MyTvMonitorApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/md_theme_light_primary</item>
        <item name="colorOnPrimary">@color/md_theme_light_onPrimary</item>
        <!-- 其他颜色属性... -->
    </style>

    <!-- 应用主题 -->
    <style name="Theme.MyTvMonitorApp" parent="Base.Theme.MyTvMonitorApp" />

    <!-- 全屏主题 -->
    <style name="Theme.MyTvMonitorApp.FullScreen" parent="Theme.MyTvMonitorApp">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>